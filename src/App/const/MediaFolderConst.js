import * as RNFS from "react-native-fs";

const LIVE = `${ RNFS.PicturesDirectoryPath }/LittleFlight_Live`;

const IMAGE = `${ RNFS.PicturesDirectoryPath }/LittleFlight_Images`;

const VIDEO = `${ RNFS.PicturesDirectoryPath }/LittleFlight_Videos`;

export default {
    LIVE,
    IMAGE,
    VIDEO,
}
