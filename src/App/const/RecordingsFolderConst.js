import * as RNFS from "react-native-fs";

const IMAGE = `${ RNFS.PicturesDirectoryPath }/LittleFlight Images`;
const VIDEO = `${ RNFS.PicturesDirectoryPath }/LittleFlight Video`;

export default {
    IMAGE,
    VIDEO,
}
