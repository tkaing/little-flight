import React from "react";
import styles from "./Styles.Remote";

import { View } from "native-base";

const RemoteView = ({ xxx }) => {

    return (
        <View />
    );
};

export default RemoteView
